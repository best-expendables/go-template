package constant

const (
	${SERVICE_NAME}RuleFirst  = "First"
	${SERVICE_NAME}RuleSecond = "Second"
)

var (
	Valid${SERVICE_NAME}Rule = map[string]bool{
		${SERVICE_NAME}RuleFirst:  true,
		${SERVICE_NAME}RuleSecond: true,
	}
)
